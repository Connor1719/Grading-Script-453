/* assignments involving expressions with two arithmetic operators */

int main() {
    int x;
    x = 1+2+3;
    x = 3-2-1;
    x = 1*2*3;
    x = 3/2/1;
    x = --1;    /* << should parse as -(-(1)) */
    x = ((x));
}
