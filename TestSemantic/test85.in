/* assignments involving expressions with function calls */

int f() {
}

int g(int x) {
    x = 0;
}

int h(int x, int y, int z, int w) {
    x = 1;
    y = 2;
    z = 3;
    w = 4;
}

int main() {
    int x;
    
    x = f();
    x = g(0);
    x = h(0, 1, 2, 3);
}
