/* boolean expressions involving single && and || operators */

int x, y;

int main() {
    if (x > y) x = 0;
    if (x > 0 && x < 10) x = 1;
    if (x == 0 || x != -1) x = 2;

    while (x > y) x = 0;
    while (x > 0 && x < 10) x = 1;
    while (x == 0 || x != -1) x = 2;
}
